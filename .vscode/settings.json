{
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",

    "terminal.integrated.profiles.linux": {
        "Bash (numathap)": {
            "path": "/bin/bash",
            "args": [
                "--rcfile",
                "${workspaceFolder}/.vscode/bashrc"
            ]
        }
    },

    "terminal.integrated.defaultProfile.linux": "Bash (numathap)"
}